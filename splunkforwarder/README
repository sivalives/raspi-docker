# This package uses a ARM base OS, add splunk UF package to it. 
# it aslo uses a volume created using docker volume create command ; Data in the iptv-volume is generated by docker-iptv container and same is read by docker-splunkuf container. 
